name: run tests for each PR

run-name: tests
on:
  pull_request:
    branches: -main
jobs:
  tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./app/

    steps:
      - uses: actions/checkout@v3

      - name: run tests
        run: npm test a
